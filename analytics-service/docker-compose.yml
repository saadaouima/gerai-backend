version: "3.9"

services:
  mailhog:
    image: mailhog/mailhog:v1.0.1
    container_name: gerai-mailhog
    ports:
      - "1025:1025"
      - "8025:8025"
    restart: unless-stopped

networks:
  default:
    external: true
    name: gerai_default