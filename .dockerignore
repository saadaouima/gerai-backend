# Build output — already built inside Docker
target/

# Environment variables — never copy into image
.env

# Git history — not needed in image
.git
.gitignore

# IntelliJ files — not needed in image
.idea/
*.iml
*.iws
*.ipr

# OS files
.DS_Store
Thumbs.db

# Docker files — not needed inside image
Dockerfile
docker-compose.yml
.dockerignore

# Logs
*.log
logs/